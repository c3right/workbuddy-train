# 项目输出目录

这个目录用于保存当前阶段生成的工作成果。

建议遵循三个原则：

1. 不修改 `00-项目背景`、`01-待处理资料` 和 `02-参考样例` 中的原始文件；
2. 第一版输出保留 `_v1`，修改后另存 `_v2`；
3. 如果结果做坏了，优先回到上一个可用版本继续，而不是覆盖原件。

当前阶段预计会逐步形成：

- `项目交接整理_v1.md`
- `区域访谈综合_v1.md`
- `内部项目简报_v1.docx`
- `内部项目简报_v2.docx`

如无必要，不要提前创建空文件。
